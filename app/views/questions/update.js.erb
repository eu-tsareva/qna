var $questionItem = $('.question__info');

$questionItem
  .find('.question__errors')
  .html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
  $questionItem.html('<%= j render(@question) %>');
<% end %>
