$('.answer__errors').html('<%= render 'shared/errors', resource: answer %>');

<% unless answer.errors.present? %>
  $('.question__answers #answer-<%= answer.id %>').html('<%= j render(answer) %>');
<% end %>
